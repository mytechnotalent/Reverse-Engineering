#pragma once

/**
 * Function to build an array
 * @param size          Size of array
 * @param init_value    Initial value of array
 * @returns arr         Built array
 */
int *build_arr(size_t size, int init_value = 0);
